name: Build & Push Docker to GHCR

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Récupération du code
        uses: actions/checkout@v4

      - name: Login to GHCR
        run:  echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Lowercase owner/repo
        run: |
          ownler_lc=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "OWNER_LC=$owner_lc" >> $GITHUB_ENV

      - name: Build image
        run: |
          docker build -t ghcr.io/$OWNER_LC/lamp-app:latest ./lamp

      - name: Push image
        run: docker push ghcr.io/$OWNER_LC/lamp-app:latest
